

const Login = () => {
    return (
        <div>
            <form action="">
            <input type="gmail" placeholder="Gmail" required className="input input-bordered w-full max-w-xs" />
            <input type="password" placeholder="password" required className="input input-bordered w-full max-w-xs" />
            </form>
        </div>
    );
};

export default Login;